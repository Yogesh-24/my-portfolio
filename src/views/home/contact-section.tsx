import { ContactForm } from "@/views/home/contact-form";

export const ContactSection = () => {
  return (
    <section id="contact" aria-label="Contact form" className="w-full">
      <ContactForm />
    </section>
  );
};